version: '3.8'

services:
  csv-agent:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./:/app
    command: python -m streamlit run app/main.py --server.port=8501 --server.address=0.0.0.0
